package org.example;

import javax.swing.*;

public class MenuAdmin extends JFrame {
    public MenuAdmin() {
        setTitle("Admin Menu");
        setSize(300, 200);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setVisible(true);
    }
}
