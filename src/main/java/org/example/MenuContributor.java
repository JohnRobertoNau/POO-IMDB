package org.example;

import javax.swing.*;

public class MenuContributor extends JFrame {
    public MenuContributor() {
        setTitle("Contributor Menu");
        setSize(300, 200);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setVisible(true);
    }
}
